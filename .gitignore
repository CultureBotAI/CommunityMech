# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
venv/
env/
ENV/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Node tooling (pulled in by docs/web preview scripts)
node_modules/

# Generated DeepWalk embeddings (large binaries; regenerated via the embedding pipeline)
data/embeddings/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/

# LinkML generated files (can be regenerated)
project/
tmp/

# OAK cache
.oak-cache/

# Reference cache (PubMed abstracts - keep in git for reproducibility)
# references_cache/

# Media cache (CultureMech/MediaIngredientMech data - can be regenerated)
media_cache/

# UMAP cache (embedding cache - can be regenerated)
.umap_cache/

# Backup files
*.bak

# Reports (generated files - can be regenerated)
reports/*.csv
reports/*.txt
reports/*.json
research/

# Generated HTML pages (can be regenerated)
pages/

# Generated browser data (can be regenerated)
app/data.js
app/hpo_category_cache.json
docs/data.js

# Large KGM knowledge graph files (too big for GitHub)
app/kgm/

# Environment
.env
.env.local
config.local.mk

# Logs
*.log

# Curation / audit pipeline outputs — these are regeneratable artifacts
# produced by scripts/curate_evidence_with_pdfs.py and the
# `audit-network-report` justfile recipe; downstream scripts read them
# within a workflow run, but they should not live in version control.
evidence_curation_report.txt
network_integrity_audit.txt

# Temporary files
*.tmp
.temp/

# Claude session data (optional - keep if useful for collaboration)
# .claude/projects/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Linux
*~

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Keep the dismech clone out of git (reference only)
dismech/

# Regenerable KGX export artifact; produced by `just kgx-export` and
# attached to releases via .github/workflows/kgx-release.yaml.
output/kgx/

# Generated per-community HTML (regenerated by `just gen-community-pages`).
pages/community/
pages/index.html
pages/style.css

# Claude Code local agent state
.claude/

# linkml-term-validator / OAK label cache
cache/
