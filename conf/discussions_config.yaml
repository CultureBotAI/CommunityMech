# CommunityMech discussions browser config — drives kg_microbe_discussions (in culturebotai-claw).
repo_name: CommunityMech
record_glob: ../kb/communities/*.yaml
name_fields: [name]
id_field: id
discussions_field: discussions
page_url_template: "../docs/communities/{stem}.html#{discussion_id}"
