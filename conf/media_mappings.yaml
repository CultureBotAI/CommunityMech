# Manual media and ingredient mapping overrides
# Used by scripts/link_growth_media.py for cases where fuzzy matching fails
# or explicit mappings are preferred.

# Media name to CultureMech ID mappings
media_overrides:
  # Example:
  # "R2A medium":
  #   culturemech_id: "CultureMech:000042"
  #   note: "Standard R2A for heterotrophs"

# Ingredient name to MediaIngredientMech ID mappings
ingredient_overrides:
  # Example:
  # "yeast extract":
  #   media_ingredient_mech_id: "MediaIngredientMech:000015"
  #   note: "Standard yeast extract component"
