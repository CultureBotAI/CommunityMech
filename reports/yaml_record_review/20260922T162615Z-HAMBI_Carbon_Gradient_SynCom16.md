# YAML Record Review: HAMBI Carbon-Gradient 16-species SynCom

- Repository: CultureBotAI/CommunityMech
- Record: `kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml`
- Started UTC: 2026-09-22T16:25:58Z
- Finished UTC: 2026-09-22T16:26:15Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `MicrobialCommunity` |
| ID | `CommunityMech:000423` |
| Label | HAMBI Carbon-Gradient 16-species SynCom |
| Path | `kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml` |
| Maintained/generated | Maintained YAML, generated by `tmp/write_hambi_carbon_gradient_syncom.py` before commit |

The target is a new curated community record for the 16-member HAMBI synthetic community from PMID:42367191 / doi:10.1093/ismeco/ycag149, serially passaged for 16 days across 96 glucose/R2A resource conditions.

## Validation

| Check | Result |
|---|---|
| `just validate kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml` | Did not reach validation. `uv` failed while building `llvmlite==0.46.0` for Python 3.13 via the optional `umap-learn` dependency. |
| `PYTHONPATH=src .venv/bin/linkml-validate -s src/communitymech/schema/communitymech.yaml kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml` | Pass, `No issues found`. |
| `PYTHONPATH=src .venv/bin/python scripts/validate_strict.py kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml` | Pass, 1 file scanned, 0 files with `ERROR`. |
| `PYTHONPATH=src .venv/bin/linkml-term-validator validate-data kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml -s src/communitymech/schema/communitymech.yaml --labels` | Pass. |
| `PYTHONPATH=src .venv/bin/linkml-reference-validator validate data kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml -s src/communitymech/schema/communitymech.yaml --config conf/reference_validator.yaml` | Pass. |
| `PYTHONPATH=src .venv/bin/python scripts/validate_gtdb_coherence.py kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml` | Pass, 0 incoherent blocks, 0 malformed lineages, 0 lineage conflicts. |
| `PYTHONPATH=src .venv/bin/python scripts/validate_prokaryotic_lineage.py kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml` | Pass, 0 contradictory lineages. |
| `PYTHONPATH=src .venv/bin/python scripts/validate_yaml_scalars.py kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml` | Pass, 0 truncated scalars. |
| `PYTHONPATH=src .venv/bin/linkml-validate --schema src/communitymech/schema/history.yaml --target-class HistoryRecord history/records/HAMBI_Carbon_Gradient_SynCom16/2026-09-22T162340Z-claude-code-cb48fa.yaml` | Pass, `No issues found`. |

## Identity and Grounding

The record's identity is coherent: the source article title, DOI, PMID, and PMCID in `references_cache/PMID_42367191.txt` match the publication represented by the three `external_resources` entries.

The record correctly treats the community as `ecological_state: ENGINEERED`, `community_origin: SYNTHETIC`, and a laboratory SynCom rather than a natural sample community. The source describes a modified 16-species synthetic community drawn from the HAMBI culture collection and used in a controlled serial-passage resource-gradient experiment.

All 16 taxonomy blocks match the member species and HAMBI strain numbers listed in the source. The local term validator accepts the NCBITaxon id/label pairs, including the locally canonical `NCBITaxon:577386` / `Niabella yanshanensis`. GTDB grounding is deliberately left `UNRESOLVED` on each member with a note that the local `NCBI2GTDB.tsv.gz` crosswalk was unavailable during curation.

An ignored/hidden-inclusive duplicate search over `kb`, `data`, `references_cache`, `history`, `docs`, and `reports`, excluding only bulky `references_cache/files/**`, found the DOI, PMID, record ID, and `HAMBI_Carbon_Gradient_SynCom16` slug only in the new cache/YAML/history/rendered artifacts for this in-flight record.

## Evidence

Supported:

- PMID:42367191 supports the 16-member HAMBI strain list, equal-volume assembly after pre-culture, 96-condition resource design, 8 total-carbon levels, 12 R2A-derived carbon fractions, 1 mL 96-well deep-well cultures, 1% transfer every 48 h, endpoint 16S profiling, and OD600 monitoring.
- The two `environmental_factors` are placed at the right scope. The source explicitly describes mixing 12 R2A-derived carbon fractions at 4 g C/L and serially diluting them to eight total-carbon concentrations from 0.031 to 4 g C/L.
- The single community-level ecological interaction avoids unsupported pairwise edges. The source reports repeatable total-carbon-driven state shifts with A. caviae, P. chlororaphis, and C. koseri dominating/co-dominating different total-carbon regimes, and discusses those shifts as competitive sorting among dominant members.
- The PubMed, DOI, and PMCID external-resource snippets are exact strings present in the committed `PMID:42367191` cache.

Unsupported or over-scoped:

- The `growth_media.temperature`, `growth_media.temperature_unit`, `growth_media.atmosphere`, `cultivation_setup.operating_temperature`, `cultivation_setup.operating_temperature_unit`, and `cultivation_setup.temperature_controlled` slots currently claim a 30 C aerobic serial-passage run. The cached source text supports 30 C for PPY pre-culture, monoculture growth assays, and pH follow-up experiments, but the serial-passage section supports only 16 days, eight 48 h growth cycles, 1% transfer into 1 mL fresh medium, and OD600 measurement after each transfer.
- The glucose-medium evidence snippet is an exact substring but is cut at `Glucose minimal medium (4 g C l`. That fragment supports only the existence of a glucose minimal medium; it does not itself support the explanation's narrower M9 + glucose preparation claim.

## Completeness

No blocker-level completeness gap was found. The record has source-backed identity, full member taxonomy, the main experimental gradients, serial-transfer cultivation volume and transfer rate, a community-level outcome claim, and stable publication links.

No pairwise interaction edges are asserted, which is correct because the inspected source does not demonstrate exact donor/recipient edges among the 16 members.

The public ENA and Zenodo deposits named in the article are not represented as `associated_datasets`. That is not a consequential defect for the community identity or interaction scope, but it is a bounded future enrichment if this record is later expanded beyond the primary publication links.

## Findings

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| F1 | Serial-passage temperature and atmosphere are over-scoped. | `kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml` assigns 30 C and `AEROBIC` to the carbon-gradient growth medium and 30 C to the serial-passage cultivation setup. The supporting source only states that the serial-passage cultures were transferred every 48 h into new 1 mL 96-well deep-well plates with the same R2A/glucose-derived carbon concentrations. The inspected 30 C text belongs to PPY pre-cultures, monoculture assays, or pH follow-up experiments. | `tmp/write_hambi_carbon_gradient_syncom.py`, then regenerate `kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml` and `docs/` |
| F2 | The glucose M9 evidence snippet is too truncated to support its explanation. | The committed snippet `Glucose minimal medium (4 g C l` is an exact substring, so the validator accepts it, but it omits the source's M9-preparation wording. The same cached paragraph contains the exact stronger text `Glucose minimal media were prepared by adding glucose stock to M9 minimal medium`. | `tmp/write_hambi_carbon_gradient_syncom.py`, then regenerate `kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml` and `docs/` |

### Minor

None found.

### Blockers

None found.

## Recommended Edits

1. In `tmp/write_hambi_carbon_gradient_syncom.py`, remove `temperature`, `temperature_unit`, and `atmosphere` from the `M9 glucose/R2A carbon-gradient media` block.
2. In `tmp/write_hambi_carbon_gradient_syncom.py`, remove `operating_temperature`, `operating_temperature_unit`, and `temperature_controlled` from the serial-passage `cultivation_setup` block.
3. In `tmp/write_hambi_carbon_gradient_syncom.py`, replace the cut glucose snippet with `Glucose minimal media were prepared by adding glucose stock to M9 minimal medium`, and optionally expand the R2A snippet to `R2A medium was prepared by dissolving R2A Broth powder (Neogen, UK) in M9 minimal medium`.
4. Regenerate the maintained YAML through `write_validated_community`, append a curation-history event describing the two review fixes, and rerender the committed HTML.

## Follow-up Checks

- Rerun direct schema, strict, term, reference, GTDB-coherence, GTDB-domain, YAML-scalar, and history validation after regeneration.
- Rerun `PYTHONPATH=src .venv/bin/python -m communitymech.render` and confirm `docs/communities/HAMBI_Carbon_Gradient_SynCom16.html`, `docs/browser.html`, and `docs/index.html` reflect the fixed YAML.
- Reinspect `kb/communities/HAMBI_Carbon_Gradient_SynCom16.yaml` for the absence of serial-passage 30 C / aerobic claims and for the stronger glucose/R2A evidence snippets.
- Run `git diff --check` before staging.

## Additional Notes

- The focused `just` validators are currently blocked at the wrapper layer by `uv` attempting to build `llvmlite==0.46.0` under Python 3.13. Direct `.venv/bin` invocations exercised the same underlying validators successfully.
- The reference validator's `Total checks: 0` output is its known zero-issues wording for a clean file, as documented in the justfile; a clean direct run was still counted as a reference-validation pass here.
- This report is read-only. It does not append a review event, mutate the YAML, regenerate history, or create GitHub issues.
